
import { MockProxy, mock } from 'jest-mock-extended'

describe('Moment', () => {

  beforeAll(() => {
    
  })

  beforeEach(() => {

  })

  afterEach(() => {

  })

  it('should ...', async () => {
    
  })
})
